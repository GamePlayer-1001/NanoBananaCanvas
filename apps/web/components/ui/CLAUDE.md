# apps/web/components/ui/

> L2 | 父级: apps/web/components/CLAUDE.md

shadcn/ui 自动管理的基础组件库 (New York 风格) — 勿手动修改，通过 `pnpm dlx shadcn@latest add <name>` 管理

## 成员清单

```
avatar.tsx        — Avatar 头像 (圆形/方形/缩写 fallback)
badge.tsx         — Badge 标签 (variant: default/secondary/destructive/outline)
breadcrumb.tsx    — Breadcrumb 面包屑导航
button.tsx        — Button 按钮 (variant + size 矩阵)
card.tsx          — Card 卡片容器 (Header/Content/Footer)
collapsible.tsx   — Collapsible 可折叠面板
dialog.tsx        — Dialog 模态框 (遮罩 + Portal)
dropdown-menu.tsx — DropdownMenu 下拉菜单
input.tsx         — Input 文本输入框
label.tsx         — Label 表单标签
progress.tsx      — Progress 进度条
scroll-area.tsx   — ScrollArea 自定义滚动容器
select.tsx        — Select 下拉选择器
separator.tsx     — Separator 分割线
sheet.tsx         — Sheet 侧边抽屉
sidebar.tsx       — Sidebar 侧边栏容器 (Provider + Trigger)
skeleton.tsx      — Skeleton 骨架屏加载态
sonner.tsx        — Sonner toast 通知 (Toaster 全局实例)
switch.tsx        — Switch 开关切换
tabs.tsx          — Tabs 标签页
tooltip.tsx       — Tooltip 工具提示
```

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
