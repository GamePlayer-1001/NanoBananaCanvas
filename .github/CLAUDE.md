# .github/
> L2 | 父级: /CLAUDE.md

成员清单
workflows/deploy.yml: GitHub Actions 生产流水线，先执行 lint/build，再用 OpenNext + Wrangler CLI 部署 Web 与 API Worker，避免依赖旧版 action 运行时。

法则: 成员完整·一行一文件·父级链接·技术词前置

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
