# 生产环境迁移清单 v1.0

> 当前状态: **Development / Test Mode**
> 迁移时机: 有真实付费用户时执行

---

## Stripe 迁移 (Test Mode → Live Mode)

- [ ] Stripe Dashboard → 切换 Live Mode 开关
- [ ] 创建 Live Mode Products (同 Test Mode 结构)
- [ ] 创建 Live Mode Prices (monthly/yearly × free/pro)
- [ ] 创建 Live Mode Credit Package Prices
- [ ] 更新环境变量:
  - `STRIPE_SECRET_KEY` → Live Mode Secret Key
  - `STRIPE_WEBHOOK_SECRET` → Live Mode Webhook Signing Secret
  - `STRIPE_PRICE_*` → Live Mode Price IDs
- [ ] 配置 Live Mode Webhook endpoint: `https://nanobananacanvas.com/api/webhooks/stripe`
  - 事件: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_succeeded`
- [ ] 测试完整支付流程 (真实信用卡)
- [ ] 验证积分到账 + 订阅状态同步

## Clerk 迁移 (Development → Production)

- [ ] Clerk Dashboard → 创建 Production Instance
- [ ] 配置生产域名 `nanobananacanvas.com` 的 OAuth 回调
- [ ] 更新环境变量:
  - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` → Production Publishable Key
  - `CLERK_SECRET_KEY` → Production Secret Key
  - `CLERK_WEBHOOK_SECRET` → Production Webhook Secret
- [ ] 配置 Production Webhook endpoint: `https://nanobananacanvas.com/api/webhooks/clerk`
  - 事件: `user.created`, `user.updated`, `user.deleted`
- [ ] 配置社交登录 OAuth (Google, Discord) 的生产回调 URL
- [ ] 启用 Clerk Frontend API proxy，确认 `/__clerk` 代理路径可访问，避免浏览器直连 `clerk.nanobananacanvas.com` 失败
- [ ] 决定是否迁移测试用户数据

## Cloudflare Workers Secrets 同步

- [ ] `wrangler secret put STRIPE_SECRET_KEY` (Live Mode)
- [ ] `wrangler secret put STRIPE_WEBHOOK_SECRET` (Live Mode)
- [ ] `wrangler secret put CLERK_SECRET_KEY` (Production)
- [ ] `wrangler secret put CLERK_WEBHOOK_SECRET` (Production)
- [ ] `wrangler secret put OPENROUTER_API_KEY` (生产用量)
- [ ] `wrangler secret put ENCRYPTION_KEY` (保持不变)

## GitHub Actions Secrets 同步

- [ ] 更新 Repository Secrets 中对应的密钥值
- [ ] 验证 CI/CD 部署流程正常

## 迁移后验证清单

- [ ] 注册新用户 → Clerk Production → D1 users 同步
- [ ] 打开 `/zh/sign-in` → DevTools Network 中 `/__clerk` 无 4xx/5xx/TLS 错误
- [ ] 订阅 Pro 套餐 → Stripe Live → 积分到账
- [ ] 执行 AI 工作流 → 积分扣费 → 退款机制
- [ ] 充值积分包 → 一次性支付 → permanent_balance 增加
- [ ] Webhook 端点响应 200 → 日志无错误

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
