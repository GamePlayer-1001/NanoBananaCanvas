# Nano Banana Canvas - 项目框架结构

> 文档版本：v3.0
> 创建日期：2024-12-24
> 更新日期：2026-02-26

---

## 一、项目目录结构

```
nano-banana-canvas/
├── apps/
│   ├── web/                              # Next.js 前端应用 (App Router)
│   │   ├── app/                          # App Router 路由目录
│   │   │   ├── [locale]/                 # i18n 动态语言前缀 (en/zh)
│   │   │   │   │
│   │   │   │   ├── (landing)/            # Landing Page 路由组（未登录用户）
│   │   │   │   │   ├── page.tsx          # / → SSG Landing Page
│   │   │   │   │   ├── pricing/
│   │   │   │   │   │   └── page.tsx      # /pricing → SSG 定价页面
│   │   │   │   │   ├── terms/
│   │   │   │   │   │   └── page.tsx      # /terms → SSG 服务条款
│   │   │   │   │   ├── privacy/
│   │   │   │   │   │   └── page.tsx      # /privacy → SSG 隐私政策
│   │   │   │   │   └── layout.tsx        # Landing 布局（深色科技风）
│   │   │   │   │
│   │   │   │   ├── (auth)/               # 认证路由组
│   │   │   │   │   ├── sign-in/
│   │   │   │   │   │   └── [[...sign-in]]/
│   │   │   │   │   │       └── page.tsx  # /sign-in → Clerk 登录页
│   │   │   │   │   ├── sign-up/
│   │   │   │   │   │   └── [[...sign-up]]/
│   │   │   │   │   │       └── page.tsx  # /sign-up → Clerk 注册页
│   │   │   │   │   └── layout.tsx        # 认证布局（左右分栏）
│   │   │   │   │
│   │   │   │   ├── (app)/                # 已登录用户路由组
│   │   │   │   │   ├── workspace/
│   │   │   │   │   │   ├── page.tsx      # /workspace → SSR 工作流列表
│   │   │   │   │   │   └── [id]/
│   │   │   │   │   │       └── page.tsx  # /workspace/:id → CSR 画布编辑器
│   │   │   │   │   ├── explore/
│   │   │   │   │   │   ├── page.tsx      # /explore → SSR 作品广场
│   │   │   │   │   │   └── [id]/
│   │   │   │   │   │       └── page.tsx  # /explore/:id → SSR 作品详情
│   │   │   │   │   ├── profile/
│   │   │   │   │   │   └── page.tsx      # /profile → SSR 个人页面
│   │   │   │   │   ├── billing/
│   │   │   │   │   │   └── page.tsx      # /billing → SSR 账单/积分
│   │   │   │   │   └── layout.tsx        # App 布局（侧边栏 + 顶部栏）
│   │   │   │   │
│   │   │   │   └── layout.tsx            # 语言布局（i18n Provider）
│   │   │   │
│   │   │   ├── api/                      # API Route Handlers
│   │   │   │   ├── users/
│   │   │   │   │   ├── me/
│   │   │   │   │   │   └── route.ts      # GET /api/users/me
│   │   │   │   │   └── me/stats/
│   │   │   │   │       └── route.ts      # GET /api/users/me/stats
│   │   │   │   │
│   │   │   │   ├── workflows/
│   │   │   │   │   ├── route.ts          # GET(列表) / POST(创建)
│   │   │   │   │   └── [id]/
│   │   │   │   │       ├── route.ts      # GET / PUT / DELETE
│   │   │   │   │       ├── publish/
│   │   │   │   │       │   └── route.ts  # POST 发布到广场
│   │   │   │   │       ├── clone/
│   │   │   │   │       │   └── route.ts  # POST 克隆
│   │   │   │   │       ├── like/
│   │   │   │   │       │   └── route.ts  # POST 点赞
│   │   │   │   │       └── favorite/
│   │   │   │   │           └── route.ts  # POST 收藏
│   │   │   │   │
│   │   │   │   ├── explore/
│   │   │   │   │   ├── route.ts          # GET 广场列表
│   │   │   │   │   └── search/
│   │   │   │   │       └── route.ts      # GET 搜索
│   │   │   │   │
│   │   │   │   ├── credits/
│   │   │   │   │   ├── balance/
│   │   │   │   │   │   └── route.ts      # GET 积分余额
│   │   │   │   │   ├── transactions/
│   │   │   │   │   │   └── route.ts      # GET 交易历史
│   │   │   │   │   └── usage/
│   │   │   │   │       └── route.ts      # GET 使用统计
│   │   │   │   │
│   │   │   │   ├── subscriptions/
│   │   │   │   │   ├── current/
│   │   │   │   │   │   └── route.ts      # GET 当前订阅
│   │   │   │   │   ├── create/
│   │   │   │   │   │   └── route.ts      # POST 创建订阅
│   │   │   │   │   └── cancel/
│   │   │   │   │       └── route.ts      # POST 取消
│   │   │   │   │
│   │   │   │   ├── tasks/
│   │   │   │   │   ├── route.ts          # GET(列表) / POST(提交)
│   │   │   │   │   └── [id]/
│   │   │   │   │       └── route.ts      # GET(状态) / DELETE(取消)
│   │   │   │   │
│   │   │   │   ├── ai/
│   │   │   │   │   ├── models/
│   │   │   │   │   │   └── route.ts      # GET 可用模型列表
│   │   │   │   │   └── execute/
│   │   │   │   │       └── route.ts      # POST 同步 AI 执行
│   │   │   │   │
│   │   │   │   ├── categories/
│   │   │   │   │   └── route.ts          # GET 分类列表
│   │   │   │   │
│   │   │   │   ├── notifications/
│   │   │   │   │   ├── route.ts          # GET 通知列表
│   │   │   │   │   └── [id]/read/
│   │   │   │   │       └── route.ts      # POST 标记已读
│   │   │   │   │
│   │   │   │   ├── files/
│   │   │   │   │   └── upload/
│   │   │   │   │       └── route.ts      # POST 文件上传
│   │   │   │   │
│   │   │   │   └── webhooks/
│   │   │   │       ├── clerk/
│   │   │   │       │   └── route.ts      # POST Clerk Webhook
│   │   │   │       └── stripe/
│   │   │   │           └── route.ts      # POST Stripe Webhook
│   │   │   │
│   │   │   ├── layout.tsx                # 根布局 (ClerkProvider + QueryProvider)
│   │   │   ├── not-found.tsx             # 404 页面
│   │   │   └── globals.css               # 全局样式
│   │   │
│   │   ├── components/                   # React 组件
│   │   │   ├── layout/                   # 布局组件
│   │   │   │   ├── app-sidebar.tsx       # 已登录侧边栏导航
│   │   │   │   ├── top-bar.tsx           # 顶部栏（搜索 + 头像）
│   │   │   │   ├── global-search.tsx     # 全局搜索
│   │   │   │   ├── user-menu.tsx         # 用户头像下拉菜单
│   │   │   │   ├── locale-switcher.tsx   # 语言切换器
│   │   │   │   └── credit-badge.tsx      # 积分余额徽章
│   │   │   │
│   │   │   ├── landing/                  # Landing Page 组件
│   │   │   │   ├── hero.tsx              # Hero 区域
│   │   │   │   ├── model-carousel.tsx    # AI 模型轮播
│   │   │   │   ├── feature-showcase.tsx  # 功能特色
│   │   │   │   ├── demo-preview.tsx      # 工作流演示
│   │   │   │   ├── pricing-table.tsx     # 定价对比表
│   │   │   │   ├── cta.tsx              # 行动召唤
│   │   │   │   └── footer.tsx           # 页脚
│   │   │   │
│   │   │   ├── canvas/                   # 画布相关组件
│   │   │   │   ├── canvas.tsx            # 主画布 (ReactFlow)
│   │   │   │   ├── canvas-controls.tsx   # 画布控制栏
│   │   │   │   ├── mini-map.tsx          # 小地图 (P2)
│   │   │   │   └── canvas-toolbar.tsx    # 画布工具栏
│   │   │   │
│   │   │   ├── nodes/                    # 节点组件
│   │   │   │   ├── base-node.tsx         # 节点基础组件
│   │   │   │   ├── text-input-node.tsx   # 文本输入节点
│   │   │   │   ├── llm-node.tsx          # LLM 生成节点
│   │   │   │   ├── display-node.tsx      # 结果展示节点
│   │   │   │   ├── image-gen-node.tsx    # 图片生成节点 (P2)
│   │   │   │   ├── video-gen-node.tsx    # 视频生成节点 (P2)
│   │   │   │   ├── audio-gen-node.tsx    # 音频生成节点 (P2)
│   │   │   │   ├── file-upload-node.tsx  # 文件上传节点 (P2)
│   │   │   │   ├── note-node.tsx         # 备注节点 (P2)
│   │   │   │   └── group-node.tsx        # 分组节点 (P2)
│   │   │   │
│   │   │   ├── edges/                    # 连线组件
│   │   │   │   └── custom-edge.tsx       # 自定义连线
│   │   │   │
│   │   │   ├── panels/                   # 侧边面板
│   │   │   │   ├── node-config-panel.tsx # 节点配置
│   │   │   │   ├── workflow-panel.tsx    # 工作流管理
│   │   │   │   └── settings-panel.tsx   # 全局设置
│   │   │   │
│   │   │   ├── menu/                     # 菜单组件
│   │   │   │   ├── context-menu.tsx      # 右键上下文菜单
│   │   │   │   └── node-menu.tsx         # 节点选择菜单
│   │   │   │
│   │   │   ├── explore/                  # 作品广场组件
│   │   │   │   ├── workflow-card.tsx     # 作品卡片
│   │   │   │   ├── workflow-grid.tsx     # 作品网格/瀑布流
│   │   │   │   ├── workflow-preview.tsx  # 只读预览
│   │   │   │   ├── category-filter.tsx  # 分类筛选
│   │   │   │   ├── sort-select.tsx      # 排序选择
│   │   │   │   ├── like-button.tsx      # 点赞
│   │   │   │   ├── favorite-button.tsx  # 收藏
│   │   │   │   ├── clone-button.tsx     # 克隆
│   │   │   │   └── report-button.tsx    # 举报
│   │   │   │
│   │   │   ├── workspace/               # 创作空间组件
│   │   │   │   ├── workflow-list.tsx     # 工作流列表
│   │   │   │   ├── workflow-item.tsx     # 工作流列表项
│   │   │   │   ├── create-button.tsx    # 创建按钮
│   │   │   │   ├── share-dialog.tsx     # 分享弹窗
│   │   │   │   └── delete-dialog.tsx    # 删除确认
│   │   │   │
│   │   │   ├── profile/                  # 个人页面组件
│   │   │   │   ├── created-works.tsx    # 已创建
│   │   │   │   ├── shared-works.tsx     # 已分享
│   │   │   │   ├── favorite-works.tsx   # 已收藏
│   │   │   │   └── account-manager.tsx  # 账号管理
│   │   │   │
│   │   │   ├── billing/                  # 计费组件（新增）
│   │   │   │   ├── credit-balance.tsx   # 积分余额卡片
│   │   │   │   ├── usage-chart.tsx      # 使用统计图表
│   │   │   │   ├── plan-card.tsx        # 套餐卡片
│   │   │   │   ├── payment-history.tsx  # 支付记录
│   │   │   │   └── topup-dialog.tsx     # 积分充值弹窗
│   │   │   │
│   │   │   └── ui/                       # shadcn/ui 组件（自动生成）
│   │   │       ├── button.tsx
│   │   │       ├── input.tsx
│   │   │       ├── dialog.tsx
│   │   │       ├── dropdown-menu.tsx
│   │   │       ├── tabs.tsx
│   │   │       ├── card.tsx
│   │   │       ├── badge.tsx
│   │   │       ├── avatar.tsx
│   │   │       ├── toast.tsx
│   │   │       ├── skeleton.tsx
│   │   │       └── ... (按需添加)
│   │   │
│   │   ├── lib/                          # 工具库
│   │   │   ├── executor/                 # 执行引擎
│   │   │   │   ├── workflow-executor.ts  # 工作流执行器
│   │   │   │   ├── node-executor.ts     # 节点执行器
│   │   │   │   └── topological-sort.ts  # 拓扑排序
│   │   │   ├── utils/                    # 通用工具
│   │   │   │   ├── id.ts               # ID 生成
│   │   │   │   ├── validation.ts       # 数据校验
│   │   │   │   └── date.ts            # 日期格式化
│   │   │   ├── query/                    # TanStack Query 配置
│   │   │   │   ├── client.ts           # Query Client
│   │   │   │   └── keys.ts            # Query Key 常量
│   │   │   ├── validations/              # Zod Schema
│   │   │   │   ├── workflow.ts
│   │   │   │   └── user.ts
│   │   │   ├── db.ts                    # D1 数据库访问层
│   │   │   ├── stripe.ts               # Stripe 服务端工具
│   │   │   └── constants.ts             # 常量定义
│   │   │
│   │   ├── stores/                       # Zustand 客户端状态
│   │   │   ├── use-flow-store.ts        # 画布/节点/连线
│   │   │   ├── use-execution-store.ts   # 执行状态
│   │   │   └── use-settings-store.ts    # 本地设置
│   │   │
│   │   ├── hooks/                        # 自定义 Hooks
│   │   │   ├── use-keyboard.ts          # 键盘事件
│   │   │   ├── use-context-menu.ts      # 右键菜单
│   │   │   ├── use-auto-save.ts         # 自动保存
│   │   │   ├── use-workflow-executor.ts # 工作流执行
│   │   │   ├── use-credits.ts           # 积分相关（新增）
│   │   │   └── use-tasks.ts             # 异步任务（新增）
│   │   │
│   │   ├── services/                     # 服务层
│   │   │   ├── ai/                       # AI 服务
│   │   │   │   ├── provider.ts          # AI Provider 抽象接口
│   │   │   │   ├── openrouter.ts        # OpenRouter
│   │   │   │   ├── deepseek.ts          # DeepSeek
│   │   │   │   ├── gemini.ts            # Google Gemini
│   │   │   │   ├── kling.ts             # Kling
│   │   │   │   └── types.ts            # AI 服务类型
│   │   │   └── storage/                  # 存储服务
│   │   │       ├── local-storage.ts     # 本地存储
│   │   │       └── export-import.ts     # 导入导出
│   │   │
│   │   ├── types/                        # TypeScript 类型
│   │   │   ├── workflow.ts              # 工作流
│   │   │   ├── node.ts                  # 节点
│   │   │   ├── edge.ts                  # 连线
│   │   │   ├── execution.ts             # 执行
│   │   │   ├── multimodal.ts            # 多模态类型系统
│   │   │   ├── user.ts                  # 用户
│   │   │   ├── explore.ts              # 广场
│   │   │   ├── credits.ts              # 积分（新增）
│   │   │   ├── task.ts                 # 异步任务（新增）
│   │   │   └── notification.ts         # 通知
│   │   │
│   │   ├── messages/                     # i18n 翻译文件（新增）
│   │   │   ├── en.json                  # English
│   │   │   └── zh.json                  # 中文
│   │   │
│   │   ├── styles/                       # 样式
│   │   │   ├── reactflow.css            # ReactFlow 覆盖
│   │   │   ├── nodes.css               # 节点样式
│   │   │   └── landing.css             # Landing 深色主题
│   │   │
│   │   ├── middleware.ts                # Next.js 中间件（auth + i18n + rate limit）
│   │   ├── i18n.ts                      # next-intl 配置
│   │   ├── next.config.ts               # Next.js 配置
│   │   ├── tailwind.config.ts           # Tailwind 配置
│   │   ├── postcss.config.js            # PostCSS 配置
│   │   ├── components.json              # shadcn/ui 配置
│   │   ├── tsconfig.json
│   │   └── package.json
│   │
│   └── worker/                           # Cloudflare Worker（AI 代理 + 异步任务）
│       ├── src/
│       │   ├── routes/
│       │   │   ├── ai-proxy.ts          # AI API 代理（流式）
│       │   │   └── task-status.ts       # 异步任务状态查询
│       │   ├── queues/
│       │   │   ├── handler.ts           # Queue 消费者入口
│       │   │   └── processors/
│       │   │       ├── base.ts          # 处理器基类
│       │   │       ├── video-gen.ts     # 视频生成处理器
│       │   │       ├── image-gen.ts     # 图片生成处理器
│       │   │       └── audio-gen.ts     # 音频生成处理器
│       │   ├── services/
│       │   │   ├── credits.ts           # 积分扣费服务
│       │   │   ├── d1.ts               # D1 数据库操作
│       │   │   ├── r2.ts               # R2 存储操作
│       │   │   └── ai-providers/       # AI 服务提供商
│       │   │       ├── openrouter.ts
│       │   │       ├── deepseek.ts
│       │   │       ├── gemini.ts
│       │   │       └── kling.ts
│       │   ├── middleware/
│       │   │   ├── cors.ts             # CORS
│       │   │   └── auth.ts             # Clerk 认证
│       │   ├── types/
│       │   │   └── index.ts
│       │   └── index.ts                 # Worker 入口
│       ├── wrangler.toml                # Cloudflare 配置
│       ├── package.json
│       └── tsconfig.json
│
├── packages/                             # 共享包
│   └── shared/
│       ├── src/
│       │   ├── types/                   # 前后端共享类型
│       │   │   ├── credits.ts          # 积分类型
│       │   │   ├── task.ts             # 任务类型
│       │   │   └── workflow.ts         # 工作流类型
│       │   └── constants/               # 共享常量
│       │       ├── plans.ts            # 套餐配置
│       │       └── models.ts           # AI 模型配置
│       ├── package.json
│       └── tsconfig.json
│
├── .md/                                  # 项目文档
│   ├── CLAUDE.MD                        # 项目规范入口
│   ├── 项目可行性分析.md
│   ├── 项目框架结构.md                   # (本文档)
│   ├── 项目规划清单.md
│   ├── 项目执行规范.md
│   ├── 多模态架构设计.md
│   ├── 积分与支付系统架构设计.md          # (新增)
│   ├── 异步任务队列架构设计.md            # (新增)
│   └── 差距分析报告.md                   # (新增)
│
├── e2e/                                  # E2E 测试 (Playwright)
│   ├── tests/
│   │   ├── canvas.spec.ts
│   │   ├── explore.spec.ts
│   │   ├── workspace.spec.ts
│   │   ├── auth.spec.ts
│   │   ├── billing.spec.ts             # (新增)
│   │   └── landing.spec.ts             # (新增)
│   ├── playwright.config.ts
│   └── package.json
│
├── .gitignore
├── .prettierrc
├── .eslintrc.js
├── sentry.client.config.ts              # Sentry 前端配置（新增）
├── sentry.server.config.ts              # Sentry 服务端配置（新增）
├── turbo.json                           # Turborepo 配置
├── package.json                         # 根 package.json (monorepo)
├── pnpm-workspace.yaml                  # pnpm workspace
└── README.md
```

---

## 二、渲染策略

### 2.1 页面渲染模式分配

| 页面 | 路由 | 渲染模式 | 理由 |
|------|------|---------|------|
| Landing Page | `/` | **SSG** | 纯静态内容，最佳 SEO + 性能 |
| 定价页面 | `/pricing` | **SSG** | 静态内容，套餐信息通过 revalidate 更新 |
| 服务条款 | `/terms` | **SSG** | 纯静态 |
| 隐私政策 | `/privacy` | **SSG** | 纯静态 |
| 登录/注册 | `/sign-in`, `/sign-up` | **CSR** | Clerk 组件客户端渲染 |
| 作品广场 | `/explore` | **SSR** | 动态内容需要 SEO，搜索引擎可索引 |
| 作品详情 | `/explore/:id` | **SSR** | 每个作品需要独立的 OG 标签 |
| 创作空间 | `/workspace` | **SSR** | 用户私有数据，服务端获取 |
| 画布编辑器 | `/workspace/:id` | **CSR** | ReactFlow 纯客户端，重交互 |
| 个人页面 | `/profile` | **SSR** | 用户数据服务端获取 |
| 账单页面 | `/billing` | **SSR** | 积分和订阅数据服务端获取 |

### 2.2 渲染模式说明

```
SSG (Static Site Generation)
├── 构建时生成 HTML
├── CDN 分发，性能最佳
├── 适用于不频繁变化的内容
└── 通过 revalidate 定期更新

SSR (Server-Side Rendering)
├── 每次请求动态生成 HTML
├── SEO 友好（搜索引擎爬到完整内容）
├── 适用于需要用户数据或频繁变化的页面
└── 运行在 Cloudflare Workers 边缘节点

CSR (Client-Side Rendering)
├── 浏览器端渲染
├── 适用于重交互页面（画布编辑器）
├── 使用 'use client' 指令
└── 不需要 SEO 的页面
```

---

## 三、页面结构说明

### 3.1 双轨路由架构

```
访问 nano-banana-canvas.com
        │
        ▼
  Next.js middleware
  (i18n 语言检测 + Clerk auth 检查)
        │
        ├── /en/ 或 /zh/ (语言前缀)
        │
    用户已登录？
        │
    ┌───┴───┐
    │       │
   否      是
    │       │
    ▼       ▼
Landing   App Layout
 Page     (Sidebar + TopBar)
    │       │
    │       ├── /workspace     → 工作流列表
    │       ├── /workspace/:id → 画布编辑器
    │       ├── /explore       → 作品广场
    │       ├── /explore/:id   → 作品详情
    │       ├── /profile       → 个人页面
    │       └── /billing       → 账单/积分
    │
    ├── /           → Hero + Features + Pricing
    ├── /pricing    → 详细定价
    ├── /terms      → 服务条款
    ├── /privacy    → 隐私政策
    ├── /sign-in    → 登录页
    └── /sign-up    → 注册页
```

### 3.2 已登录用户布局

```
┌──────────────────────────────────────────────────────────────┐
│  Logo    │  🔍 搜索作品广场...       │ 💎 1,200 │  🌐 │ 👤  │ ← TopBar
├──────────┼───────────────────────────────────────────────────┤
│  首页    │                                                   │
│          │                                                   │
│  广场    │                                                   │
│          │              主内容区域                            │
│ 创作空间 │             (页面内容)                             │
│          │                                                   │
│  个人    │                                                   │
│          │                                                   │
│  账单    │                                                   │
│    ↑     │                                                   │
│ Sidebar  │                                                   │
└──────────┴───────────────────────────────────────────────────┘
          💎 = 积分余额   🌐 = 语言切换   👤 = 用户菜单
```

### 3.3 页面路由表

#### 公开路由（无需登录）

| 页面 | 路由 | 渲染 | 说明 |
|------|------|------|------|
| Landing | `/{locale}` | SSG | 营销展示页 |
| 定价 | `/{locale}/pricing` | SSG | 套餐对比 |
| 服务条款 | `/{locale}/terms` | SSG | 法律页面 |
| 隐私政策 | `/{locale}/privacy` | SSG | 法律页面 |
| 登录 | `/{locale}/sign-in` | CSR | Clerk 登录 |
| 注册 | `/{locale}/sign-up` | CSR | Clerk 注册 |
| 作品广场 | `/{locale}/explore` | SSR | 公开浏览（可选登录） |
| 作品详情 | `/{locale}/explore/:id` | SSR | 公开浏览（可选登录） |

#### 受保护路由（需登录）

| 页面 | 路由 | 渲染 | 说明 |
|------|------|------|------|
| 创作空间 | `/{locale}/workspace` | SSR | 工作流列表 |
| 画布编辑器 | `/{locale}/workspace/:id` | CSR | ReactFlow 画布 |
| 个人 | `/{locale}/profile` | SSR | 个人信息 |
| 账单 | `/{locale}/billing` | SSR | 积分/订阅管理 |

---

## 四、核心模块说明

### 4.1 Next.js 中间件

```typescript
// middleware.ts
import { clerkMiddleware, createRouteMatcher } from '@clerk/nextjs/server'
import createMiddleware from 'next-intl/middleware'

const intlMiddleware = createMiddleware({
  locales: ['en', 'zh'],
  defaultLocale: 'en',
})

const isProtectedRoute = createRouteMatcher([
  '/:locale/workspace(.*)',
  '/:locale/profile(.*)',
  '/:locale/billing(.*)',
])

export default clerkMiddleware(async (auth, req) => {
  // i18n 路由处理
  const response = intlMiddleware(req)

  // 受保护路由检查
  if (isProtectedRoute(req)) {
    await auth.protect()
  }

  return response
})

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)'],
}
```

### 4.2 根布局

```typescript
// app/layout.tsx
import { ClerkProvider } from '@clerk/nextjs'
import { QueryProvider } from '@/lib/query/provider'

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider>
      <QueryProvider>
        {children}
      </QueryProvider>
    </ClerkProvider>
  )
}
```

### 4.3 App 布局（已登录用户）

```typescript
// app/[locale]/(app)/layout.tsx
import { AppSidebar } from '@/components/layout/app-sidebar'
import { TopBar } from '@/components/layout/top-bar'

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen">
      <AppSidebar />
      <div className="flex-1 flex flex-col">
        <TopBar />
        <main className="flex-1 overflow-auto">
          {children}
        </main>
      </div>
    </div>
  )
}
```

### 4.4 画布编辑器页面（CSR）

```typescript
// app/[locale]/(app)/workspace/[id]/page.tsx
'use client'

import dynamic from 'next/dynamic'

// ReactFlow 必须客户端渲染，禁用 SSR
const CanvasEditor = dynamic(
  () => import('@/components/canvas/canvas-editor'),
  { ssr: false }
)

export default function EditorPage({ params }: { params: { id: string } }) {
  return <CanvasEditor workflowId={params.id} />
}
```

---

## 五、后端架构

### 5.1 职责划分

| 组件 | 运行环境 | 职责 |
|------|---------|------|
| Next.js Route Handlers | Cloudflare Pages Functions | CRUD API、积分管理、用户管理、Webhook |
| Cloudflare Worker | Cloudflare Workers | AI API 代理（流式/长连接）、异步任务消费 |
| Cloudflare Queues | Cloudflare 边缘 | 异步任务队列（视频/图片生成） |
| Cloudflare D1 | Cloudflare 边缘 | SQLite 数据库 |
| Cloudflare R2 | Cloudflare 边缘 | 对象存储（生成内容、用户文件） |

### 5.2 数据流

```
┌─────────────────────────────────────────────────────────────┐
│                      Next.js Frontend                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ Landing  │  │ Explore  │  │ Canvas   │  │  Billing   │ │
│  │  (SSG)   │  │  (SSR)   │  │  (CSR)   │  │   (SSR)    │ │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘ │
│                        │                                    │
│  ┌─────────────────────┴─────────────────────────────────┐ │
│  │          Next.js API Route Handlers                    │ │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────────────┐ │ │
│  │  │ CRUD   │ │Credits │ │Webhook │ │  Task Submit   │ │ │
│  │  │  API   │ │  API   │ │Handler │ │    (→Queue)    │ │ │
│  │  └───┬────┘ └───┬────┘ └───┬────┘ └───────┬────────┘ │ │
│  └──────┼──────────┼──────────┼───────────────┼──────────┘ │
└─────────┼──────────┼──────────┼───────────────┼────────────┘
          │          │          │               │
    ┌─────┴──────────┴──────────┴───┐   ┌──────┴──────┐
    │       Cloudflare D1            │   │  Cloudflare │
    │      (SQLite Database)         │   │   Queues    │
    └────────────────────────────────┘   └──────┬──────┘
                                                │
                                        ┌───────┴────────┐
                                        │  Cloudflare    │
                                        │   Worker       │
                                        │  (AI Proxy +   │
                                        │  Task Consumer)│
                                        └───┬────────┬───┘
                                            │        │
                                     ┌──────┴──┐ ┌───┴──────┐
                                     │   R2    │ │  AI APIs  │
                                     │ Storage │ │(OpenRouter│
                                     └─────────┘ │ Gemini..)│
                                                  └──────────┘
```

### 5.3 Worker wrangler.toml

```toml
name = "nano-banana-canvas-worker"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# 环境变量
[vars]
CLERK_SECRET_KEY = ""
STRIPE_SECRET_KEY = ""
OPENROUTER_API_KEY = ""

# D1 数据库
[[d1_databases]]
binding = "DB"
database_name = "nanobanana-canvas"
database_id = "<id>"

# R2 存储
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "nano-banana-canvas-files"

# Queues 生产者
[[queues.producers]]
queue = "ai-tasks"
binding = "AI_TASK_QUEUE"

# Queues 消费者
[[queues.consumers]]
queue = "ai-tasks"
max_batch_size = 10
max_batch_timeout = 30
max_retries = 3
dead_letter_queue = "ai-tasks-dlq"
```

---

## 六、组件依赖关系

```
app/layout.tsx (根布局)
├── ClerkProvider
├── QueryProvider
│
├── app/[locale]/layout.tsx (语言布局)
│   ├── NextIntlClientProvider
│   │
│   ├── (landing)/layout.tsx
│   │   ├── LandingNav (Logo + 导航 + 登录按钮 + 语言切换)
│   │   └── Outlet
│   │       ├── LandingPage → Hero + ModelCarousel + FeatureShowcase + PricingTable + CTA + Footer
│   │       ├── PricingPage → PricingTable (详细版)
│   │       ├── TermsPage
│   │       └── PrivacyPage
│   │
│   ├── (auth)/layout.tsx
│   │   └── Outlet
│   │       ├── SignInPage → Clerk <SignIn />
│   │       └── SignUpPage → Clerk <SignUp />
│   │
│   └── (app)/layout.tsx
│       ├── AppSidebar → NavItem[] + CreditBadge
│       ├── TopBar → GlobalSearch + LocaleSwitcher + CreditBadge + UserMenu
│       └── Outlet
│           ├── WorkspacePage → CreateButton + WorkflowList → WorkflowItem[]
│           ├── EditorPage → CanvasEditor (dynamic, ssr: false)
│           │   ├── CanvasToolbar
│           │   └── Canvas (ReactFlow)
│           │       ├── Nodes (TextInput, LLM, Display, ...)
│           │       ├── Edges (CustomEdge)
│           │       └── ContextMenu
│           ├── ExplorePage → CategoryFilter + SortSelect + WorkflowGrid → WorkflowCard[]
│           ├── ExploreDetailPage → WorkflowPreview + LikeButton + FavoriteButton + CloneButton
│           ├── ProfilePage → Tabs (CreatedWorks, SharedWorks, FavoriteWorks, AccountManager)
│           └── BillingPage → CreditBalance + UsageChart + PlanCard + PaymentHistory
```

---

## 七、技术栈版本

### 核心框架

| 依赖 | 版本 | 说明 |
|------|------|------|
| Next.js | ^15.0.0 | React 全栈框架 (App Router) |
| React | ^19.0.0 | UI 框架 |
| TypeScript | ^5.5.0 | 类型系统 |
| ReactFlow | ^12.0.0 | 画布引擎 |
| Zustand | ^5.0.0 | 客户端状态管理 |
| Tailwind CSS | ^4.0.0 | 样式框架 |

### 工程化

| 依赖 | 版本 | 说明 |
|------|------|------|
| Turborepo | ^2.0.0 | Monorepo 编排 |
| pnpm | ^9.0.0 | 包管理器 |
| `@opennextjs/cloudflare` | latest | Next.js → Cloudflare 适配器 |

### 数据与表单

| 依赖 | 版本 | 说明 |
|------|------|------|
| TanStack Query | ^5.0.0 | 服务端状态 |
| React Hook Form | ^7.48.0 | 表单处理 |
| Zod | ^3.22.0 | 数据验证 |

### UI 组件

| 依赖 | 版本 | 说明 |
|------|------|------|
| shadcn/ui | latest | 可复制组件库 |
| Radix UI | latest | 无障碍基础组件 |
| Lucide React | latest | 图标库 |

### 认证与支付

| 依赖 | 版本 | 说明 |
|------|------|------|
| `@clerk/nextjs` | ^6.0.0 | Clerk Next.js SDK |
| `stripe` | ^14.0.0 | Stripe 服务端 |
| `@stripe/stripe-js` | ^2.0.0 | Stripe 前端 |

### 国际化

| 依赖 | 版本 | 说明 |
|------|------|------|
| `next-intl` | ^3.0.0 | Next.js i18n |

### 测试

| 依赖 | 版本 | 说明 |
|------|------|------|
| Vitest | ^2.0.0 | 单元测试 |
| Playwright | ^1.40.0 | E2E 测试 |

### 监控

| 依赖 | 版本 | 说明 |
|------|------|------|
| `@sentry/nextjs` | ^8.0.0 | 错误监控 |

### 后端

| 依赖 | 版本 | 说明 |
|------|------|------|
| Hono | ^4.0.0 | Worker 路由框架 |
| `@clerk/backend` | ^1.0.0 | Clerk 后端验证 |

---

## 八、环境配置

### Next.js 环境变量

```env
# .env.local
NEXT_PUBLIC_APP_NAME=Nano Banana Canvas
NEXT_PUBLIC_APP_URL=https://nanobananacanvas.com

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_xxx
CLERK_SECRET_KEY=sk_xxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# Stripe
STRIPE_SECRET_KEY=sk_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Cloudflare
CLOUDFLARE_D1_DATABASE_ID=xxx
CLOUDFLARE_R2_BUCKET=nano-banana-canvas-files

# Worker API (AI 代理)
NEXT_PUBLIC_WORKER_API_URL=https://worker.nanobananacanvas.com

# Sentry
SENTRY_DSN=https://xxx@sentry.io/xxx
```

### Worker 环境变量

```toml
# wrangler.toml
[vars]
CLERK_SECRET_KEY = ""
STRIPE_SECRET_KEY = ""
OPENROUTER_API_KEY = ""
DEEPSEEK_API_KEY = ""
GEMINI_API_KEY = ""
KLING_API_KEY = ""
CORS_ORIGIN = "https://nanobananacanvas.com"
```

---

## 九、设计规范

### 9.1 白色系配色（App 内部）

```css
:root {
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #f9fafb;
  --color-bg-tertiary: #f3f4f6;
  --color-border: #e5e7eb;
  --color-border-hover: #d1d5db;
  --color-text-primary: #111827;
  --color-text-secondary: #6b7280;
  --color-text-tertiary: #9ca3af;
  --color-primary: #3b82f6;
  --color-primary-hover: #2563eb;
  --color-success: #22c55e;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
}
```

### 9.2 Landing Page 深色科技风

```css
:root {
  --landing-bg-start: #0B0B0F;
  --landing-bg-end: #000000;
  --landing-text-primary: #ffffff;
  --landing-text-secondary: rgba(255, 255, 255, 0.7);
  --landing-accent: #3b82f6;
  --landing-card-bg: rgba(255, 255, 255, 0.05);
  --landing-card-border: rgba(255, 255, 255, 0.1);
}
```

### 9.3 响应式断点

| 断点 | 宽度 | 说明 |
|------|------|------|
| `sm` | 640px | 手机横屏 |
| `md` | 768px | 平板竖屏 |
| `lg` | 1024px | 平板横屏 / 小笔记本 |
| `xl` | 1280px | 桌面 |
| `2xl` | 1536px | 大屏 |

**响应式策略**：
- Landing Page：全响应式（手机 → 桌面）
- 作品广场 / 个人页面：响应式布局
- 画布编辑器：**桌面端专属**，`< lg` 显示"请使用电脑访问"提示

---

## 十、更新日志

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2024-12-24 | v1.0 | 初始版本 |
| 2024-12-24 | v2.0 | P1 平台化升级 |
| 2024-12-24 | v2.1 | 结构调整：个人页面 |
| 2024-12-24 | v2.2 | 新增作品详情页 |
| 2024-12-24 | v2.3 | 工程化升级 |
| 2026-02-26 | **v3.0** | **架构重构**：Vite SPA → Next.js App Router；新增 i18n 支持 (next-intl)；新增积分/计费模块 (Stripe)；新增异步任务队列 (Cloudflare Queues)；新增 AI 代理 Worker；新增法律页面；新增响应式设计策略；更新技术栈版本；更新环境配置 |

---

[PROTOCOL]: 变更时更新此文档，然后检查 CLAUDE.md
